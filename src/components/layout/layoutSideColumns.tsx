import { spacingPx } from '@constants/theme'
import { View } from 'react-native'
import { styled } from 'styled-components'

export const LayoutSideColumns = styled(View)`
  margin: 0 ${spacingPx.m};
`
